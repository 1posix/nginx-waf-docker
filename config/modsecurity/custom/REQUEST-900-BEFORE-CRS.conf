# Local request exclusions / virtual patches loaded BEFORE CRS.
#
# Keep exceptions as narrow as possible: target one rule + one parameter/path
# rather than disabling a whole rule group globally.
#
# Example (disabled): remove a specific CRS rule for a single endpoint.
# SecRule REQUEST_URI "@beginsWith /trusted-api/" #     "id:1000001,phase:1,pass,nolog,ctl:ruleRemoveById=941100"
